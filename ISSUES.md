# Issues

> **Defects only.** An open item must be broken, dead, wrong, or contradict the docs.
> Cosmetic drift → `CONSIDERATIONS.md`. Features and answered design decisions → `ROADMAP.md`.
> One bullet per root cause, with the affected sites listed inside it — never one bullet per site.


## Open
- [ ] **Existing archived posts still hold relative strings in `posted_at`** — the connector fix (CONSIDERATIONS Q16) only applies from the next crawl onward. Any YouTube community post already in `posts`/`posts_fts` keeps whatever text it was stored with, so those rows still sort wrongly and still slip through date filters. A backfill would have to resolve each relative string against the *crawl* time rather than now, which means the run timestamp has to be recoverable for the row — check `core/run_history` before assuming it is. Not attempted: it rewrites existing archive data, which is Stuart's call, not a cleanup to do unasked. *(found 2026-09-11)*
## Resolved
- [x] **Browse prints "N of M match(es)" but renders only the first page and offers no way to reach the rest** — decided: infinite scroll. **FIXED — `/api/search` and `PostIndex.search` already paged (`limit`/`offset`/`has_more`); only the client ignored it. `static/js/browse.js` now keeps `searchLimit`/`searchOffset`/`hasMore`, `search()` fetches page 0 and `moreResults()` appends the next page, driven by a native `IntersectionObserver` on a sentinel below the results list (`root` is the stage's own `x-ref="scroller"` region, not the window, because the page shell is `h-screen overflow-hidden` with one inner scroller; `rootMargin: 200px` prefetches before the user hits the floor). The sentinel is `x-show="results.length > 0"`, so a `display:none` element can never intersect and fire a spurious load on an empty page; it doubles as the "Loading more…" / "End of results" line. The match-count line no longer hides while `searching`, which would otherwise blink on every append. Observer is attached in `$nextTick` — `x-init` on `<body>` runs before Alpine walks to the child `x-ref`s. New `test_search_pages_with_limit_and_offset` in `tests/test_index.py` asserts paging neither repeats nor drops a row; 78 tests pass, `/browse` renders 200.** *(2026-09-11)*
- [x] **Browse's search reports "N of M match(es)" but offers no way to see the remaining M−N** — same defect as the item above, fixed by the same infinite-scroll change *(2026-09-11)*
- [x] **`templates/account.html` ported to the shared Alpine `page()` architecture** — decided: Do this. **ALREADY DELIVERED — `templates/account.html` is 104 lines of `x-data="page()"` markup against `static/js/account.js`'s `page()` factory, with the post list fetched from `/api/posts` and paged client-side. Nothing left to port; verified against the template, not assumed.** *(2026-09-11)*
- [x] **The Dashboard's run-status pill has no fallback class, so an unrecognised status renders unstyled** — `templates/index.html:54` binds `:class="run.status"` raw. `static/base.css` only styles `running`/`error`/`done`/`idle`, so any other value (or a null status) yields a bare `.status` with no background, colour or border — a shapeless label in a column of pills. `static/js/run.js:54-58` solves exactly this with a lookup that falls back to `'idle'`; `static/js/dashboard.js` has no equivalent. Fix: map the status through the same fallback before binding. *(found 2026-09-06)* — **FIXED — moved the neutral treatment onto the base `.status` rule in `static/base.css`, so a status this stylesheet does not know about (a new state, or a null) still renders as a pill instead of bare unstyled text. `templates/index.html:54` binds `run.status` raw from the API, so unknown values are always possible. `.status.idle` deleted — it was byte-identical to the new base. 77 tests pass** *(2026-09-11)*
- [x] **`youtube_community` posts carry relative timestamps ("1 month ago") instead of ISO dates** — the connector stores whatever YouTube renders, so `posted_at` is a human string. Consequences: `account.html`'s stats slice it to 10 chars and print `9 months a`; date-range filters and export's `since`/`until` (which compare lexically against ISO strings) silently exclude every youtube_community post; index ordering is alphabetical, not chronological. Fix belongs in `connectors/youtube_community.py` — resolve the relative string to an absolute date at crawl time, then `python main.py reindex`. *(found 2026-09-04)* — **FIXED — Stuart's answer to CONSIDERATIONS Q16: absolute dates only, never relative. Added `_resolve_relative_timestamp()` to `connectors/youtube_community.py`, which resolves YouTube's human text against the scrape time at ingest and stores ISO 8601 (`%Y-%m-%dT%H:%M:%SZ`, the format `core/feeds.py` already uses). Handles numeric counts, the word forms YouTube uses (`a minute ago`, `an hour ago`), an `Edited ` prefix, and any case. Anything resolved this way sets `timestamp_estimated=True` — the flag already existed on `Item` — because '1 month ago' is a month-wide claim. An unparseable value is stored EMPTY and logged, never guessed. The raw string is untouched in `Item.raw`. **Two consequences beyond the display bug the ticket described:** `core/index.py` does `ORDER BY posted_at` on text, so relative strings sorted alphabetically; and `core/orchestrator.py`'s date filter calls `datetime.fromisoformat()` and KEEPS the item on `ValueError`, so a date range silently matched every YouTube post. Both are fixed by the format change. `templates/account.html:45,51` slice `[:10]`, which now yields `2026-09-11` instead of `9 months a` — that corrects itself. Also fixed a latent `NameError`: the module referenced no `logger` at all, so the new warning path would have crashed; added `logger = logging.getLogger("bytebytego")` matching `main.py:34`. The new test caught it. New `tests/test_youtube_community.py` (13 cases) covers each unit, the word forms, case-insensitivity, ISO pass-through staying non-estimated, unparseable-returns-empty, and that output is both lexicographically sortable and `fromisoformat`-parseable. Full suite 77 passed. Standing rule recorded in `UX.md`.** *(2026-09-11)*
- [x] **`.target-row .target-key` and `.target-row .target-count` rules in `static/base.css:308-319` are dead CSS** — neither `templates/stages/configure.html:42-46` nor `templates/stages/run.html:59-64` uses these classes; both apply `class="flex-1 font-mono text-xs truncate"` directly to the `<span>`. The two CSS rules are unused. Fix: delete them, or adopt them in the templates. *(found 2026-09-10)* — **FIXED — deleted both rules from `static/base.css`; neither class appears in any template** *(2026-09-11)*
- [x] **`.source-tabs` in `static/base.css:331-353` is dead CSS** — the comment says "Source-tab strip inside Connect stage", but `templates/stages/connect.html:24-32` renders status pills in a plain flex row and never uses `.source-tabs`. 23 lines of underline-tab styling (hover, `.active`, top-corner radii) that nothing matches. Fix: delete it, or apply it — see the matching STYLE.md question about which selector presentation Connect and Storage should share. *(found 2026-09-06)* — **WRONG — `.source-tabs button` is live. `connect.html:24-28` and `storage.html:25-29` both render real `<button>` elements inside `.source-tabs` and toggle `'active'` on them, which is exactly what the rules target** *(2026-09-11)*
- [x] **`status-done` is not a class that exists — the selected source/backend pill renders identically to the unselected ones** — `templates/stages/connect.html:28` and `templates/stages/storage.html:30` bind `:class="… ? 'status-done' : 'idle'"`, but `static/base.css:188` defines `.status.done`, not `.status-done`. `static/js/run.js:49-58` maps correctly to `'done'`. Net effect: on Connect there is no visual indication of which source tab is active, and on Storage no indication of which backend is selected. Fix: use `'done'` in both bindings. *(found 2026-09-06)* — **STALE — both templates now bind `:class="... ? 'active' : ''"`, not `status-done`. Already fixed** *(2026-09-11)*
- [x] **`peer-checked:status-done` on Storage's backend pill is a no-op Tailwind variant** — `templates/stages/storage.html:29` writes `peer-checked:status-done peer-checked:border-0`; Tailwind variants can only prefix Tailwind utilities, not arbitrary project class names, so `peer-checked:status-done` generates nothing. The `peer`/`hidden` radio scaffolding is also redundant because the sibling `:class` binding on line 30 already drives the selected state. Fix: drop the two `peer-checked:*` utilities. *(found 2026-09-06)* — **STALE — `storage.html:27-29` no longer uses `peer-checked:status-done`; it is a plain `<button>` toggling `'active'`** *(2026-09-11)*
- [x] **Dashboard's "Recent runs" empty state still uses faint text instead of the decided panel-p5-with-h3 convention** — `templates/index.html:46` is `<p class="text-xs" style="color:var(--faint)">No runs yet. Go to Run to start one.</p>`. The decision on 2026-09-07 said the bordered `.panel p-5` convention applies "everywhere", but the previous commit (`da61ded`) was scoped to Configure/Run/Schedule only and left this one. Fix: convert to the same `panel p-5` + h3 + muted guidance pattern used on Browse and account. *(found 2026-09-10)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Five colour literals sit outside the `:root` token set** — `static/base.css:253-254` (`.account-card:hover` → `#1c1f2b` / `#414660`) and `:363-364` (`.post:hover` → the same pair) duplicate an unnamed hover surface; `:396` (`.post-text`) uses `#cbd5e1`, a sixth grey that is neither `--text` (`#e2e8f0`) nor `--muted`; `:111` and `:269` use bare `#fff` for headings. Fix: add `--panel-hover` and `--border-hover` tokens for the hover pair, use `--text` for `.post-text`, and give the white heading colour a token. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Panel section headings are hand-written longhand in eight places and their bottom margins have drifted** — `h2 class="text-sm font-semibold text-white mb-N"` appears in `browse.html:44,84` (`mb-2`), `index.html:44,60` (`mb-2`), `run.html:28,66` (`mb-3`), `configure.html:26,48` (`mb-3`), `schedule.html:26,52` (`mb-3`), so the same heading sits 4px lower on some stages than others. Fix: define one `.panel-title` in `static/base.css` with a single margin and use it everywhere. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **`style="cursor:pointer"` is repeated on twelve buttons that already get it from the stylesheet** — `static/base.css:85` sets `cursor: pointer` on `button.stage-actions`, yet the inline override is repeated at `browse.html:48,64`, `account.html:98`, `connect.html:29,57,61`, `configure.html:40,67`, `run.html:41`, `schedule.html:59,63`, `storage.html:103,107`. Fix: delete the inline styles. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Faint text and inline links are styled with a repeated inline `style` attribute instead of a class** — `style="color:var(--faint)"` appears ~14 times and `style="color:var(--accent)"` ~12 times across `index.html`, `account.html` and every `templates/stages/*.html`, even though `.field-hint` already exists for the first case and nothing exists for the second. Fix: add `.text-faint` and `.link` (or equivalent) to `static/base.css` and replace the inline attributes. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Storage is the only stage whose panel opens without a heading** — every other stage's `.panel` starts with an `<h2>` section title (`connect.html:37`, `configure.html:26,48`, `run.html:28,66`, `schedule.html:26,52`, `browse.html:44`), but `templates/stages/storage.html:23-24` drops straight from `.panel` into `<label class="field-label">Backend</label>`, so the page reads as if the heading were lost. Fix: add a section heading matching the others. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **The nav's action slot renders empty on every page, and six templates carry a redundant `{% with %}` to keep it that way** — `_nav.html:14` reserves a `.stage-actions` container for a per-stage action, `web.py:69` already defaults `stage_actions` to `''`, and `account.html:15`, `browse.html:13`, `connect.html:13`, `configure.html:13`, `run.html:13`, `schedule.html:13`, `storage.html:13` each wrap the include in `{% with stage_actions = '' %}` to set it to the value it already has. The slot is dead in all seven pages. Fix: either drop the slot and the seven `{% with %}` wrappers, or populate it. *(found 2026-09-06)* — **CLOSED closed as cosmetic drift, not a defect. ISSUES.md is for things that are broken, dead, wrong, or contradict the docs; token/spacing/casing/naming drift with no functional effect does not qualify** *(triaged 2026-09-11)*
- [x] **Empty-state panels introduced for Configure/Run/Schedule nest inside their parent panels and create double borders that don't match Browse or account.html** — `da61ded` added `.panel p-5` empty states inside the existing `Targets` `.panel p-4 mb-5` (`configure.html:33`), the `Status banner` `.panel p-4 mb-5` (`run.html:50`), the `Recent runs` `.panel p-4` (`run.html:73`), and the `Current state` `.panel p-4` (`schedule.html:46`). Browse's account empty state (`browse.html:90`) and account.html's post empty state (`account.html:88`) are top-level panels, not nested, so the convention reads "empty-state panel is a sibling of the populated cards, not a child of a containing panel". Fix: move each empty-state panel out as a sibling, or drop the outer `.panel` wrapper when the list is empty. *(found 2026-09-10)* — auto-continue *(resolved 2026-09-10)*
- [x] **Empty states are presented two different ways: Browse and the account page draw a bordered `.panel` with a heading and a sentence of guidance, while Configure, Run and Schedule print a single line of faint text. Which should be the convention for an empty list?** — decided: a — bordered .panel p-5 with an h3 heading and a sentence of guidance, everywhere (bring Configure, Run and Schedule up to match Browse/account). Implement this decision. *(answered 2026-09-07)* — auto-continue *(resolved 2026-09-10)*
- [x] **Connect's source selector and Storage's backend selector are both built out of `.status` pills, while `static/base.css:331-353` carries an unused `.source-tabs` underline-tab component written for exactly this job. Which presentation should both selectors use — the pills that ship today, or the tab strip the stylesheet already describes?** — decided: b — adopt the existing .source-tabs underline strip on both Connect and Storage; drop the .status pill usage for selection. Implement this decision. *(answered 2026-09-07)* — auto-continue *(resolved 2026-09-07)*
- [x] **`python main.py export` **got a dashboard entry point**: Browse's** — decided: This doesn't make sense?. Implement this decision. *(answered 2026-09-05)* — auto-continue *(resolved 2026-09-06)*
- [x] **Every stage page's primary button rendered unstyled — `.stage-actions` only ever matched descendants** — `static/base.css:72` defined `.stage-actions a, .stage-actions button`, i.e. the class as a *container* (which is how `_nav.html:14` uses it), but every stage page puts `class="stage-actions"` directly *on* the button (`browse.html`, `connect.html`, `run.html`, `schedule.html`, `storage.html`). Those buttons matched no rule and rendered as bare browser default text. Fixed by adding `a.stage-actions, button.stage-actions` to the same three rule sets, so both forms work. *(found + resolved 2026-09-04)*
- [x] **Browse's search reported "0 of 0 match(es)" when the real problem was a missing index** — `/api/search` returns an `error` field ("No index yet — run a crawl or `python main.py reindex`") but `static/js/browse.js` read only `results`/`total`, so an unbuilt `index.db` was indistinguishable from a genuine no-hits search. Now surfaced in a `.field-error` line, and the match count is suppressed when there's an error. *(found + resolved 2026-09-04)*
- [x] **Archived post text rendered raw HTML entities (`&#32;`) on the account page** — Reddit's text arrives entity-encoded; the old page ran it through Jinja's `striptags`, the ported client-side render did a regex tag strip that left entities intact. `postText()` now parses to a detached document and takes `textContent`, which strips tags and decodes entities in one step (and executes nothing). *(found + resolved 2026-09-04)*
- [x] **Account and Browse rendered `@@ByteByteGo`** — both prefixed a literal `@` to an account name that already had one for some sources. Added a `handle()` helper on Browse and an `lstrip('@')` on the account page. *(found + resolved 2026-09-04)*
- [x] **Connect stage can't configure a source that has no existing `sources:` block in config.yaml** — `GET /api/config/sources` (`web.py:648-650`) returns `sources: cfg.get("sources") or {}` verbatim, so `draft[source]` (`static/js/connect.js`'s `reload()`) is only populated for sources already present in `config/config.yaml`. `mastodon` and `bluesky` (and pre-existing `youtube_community`) have no block there, so clicking their Connect tab falls into `templates/stages/connect.html:49`'s `!draft[active]` branch and shows "Pick a source above" instead of an editable form — there's no way to enter a Mastodon token or Bluesky app password from the UI at all. Fix: have the endpoint (or the client) synthesize an empty config object for every source in `available` that isn't already in the YAML block. — fixed in `70b6002`: `/api/config/sources` now returns a block (from `_SOURCE_DEFAULTS`) for every available source *(resolved 2026-08-31)*
- [x] **Storage stage's Azure field label breaks the "`<Provider> <field>`" naming pattern used for S3/GCS** — `templates/stages/storage.html:51` and `:72` label the bucket field `S3 bucket` and `GCS bucket` respectively, but the equivalent Azure field at `templates/stages/storage.html:88` is just labelled `Container`, with no `Azure` prefix. Fix: rename it to `Azure container` to match the other two backends. — auto-continue *(resolved 2026-08-31)*
- [x] **Connect stage has no hint/secret metadata for the Mastodon and Bluesky connectors** — `static/js/connect.js`'s `HINTS` (line 3-9) and `SECRETS` (line 12-15) objects list one entry per existing source (`youtube_community`, `reddit`, `rss`, `twitter`, `facebook`) but have no entries for `mastodon` or `bluesky`, so those sources show a blank hint and never mask their `token`/`app_password` fields as secrets on that stage. Fix: add `mastodon: '...'`/`bluesky: '...'` hints and mark their token/password fields as secrets, matching the pattern used for `facebook`'s `graph_token`. — auto-continue *(resolved 2026-08-31)*
- [x] **Configure stage's source dropdown is a hardcoded list that's missing the Mastodon and Bluesky connectors** — `static/js/configure.js:6` literally lists `['facebook', 'reddit', 'rss', 'twitter', 'youtube_community']` instead of reading from `core/registry.py`'s `available_sources()` (already exposed dynamically to Connect via `/api/config/sources`'s `available` field, per `static/js/connect.js:36`). Since `mastodon` and `bluesky` were added to the registry (`core/registry.py`) but never added here, there's no way to add a Mastodon or Bluesky crawl target from the Configure UI. Fix: source this list from the same `available` data Connect already uses, or add an equivalent server-side endpoint. — auto-continue *(resolved 2026-08-31)*
- [x] **`account.html` renders Twitter-only metric field names for every platform** — `templates/account.html:234-236` and `:333-335` read `post.metrics.likes` / `.retweets` / `.replies`, but `Item.metrics` is source-specific (Reddit uses `score`/`num_comments`; the new Mastodon connector uses `favourites`/`reblogs`/`replies` per `connectors/mastodon.py`). Non-Twitter posts silently show 0 for all three. Fix: map each source's actual metric keys (or fall back gracefully) instead of assuming Twitter's names. — auto-continue *(resolved 2026-08-31)*
- [x] **Search results link to the account page, not the specific post** — `templates/stages/browse.html:58` builds `href="/account/" + r.platform + "/" + r.account"`, dropping the `r.post_id` that's already available in the search result. The roadmap explicitly calls for results to "link to the post in the account view." Fix: add a `#post-<id>` fragment to the link and a matching `id="post-<id>"` on each post `<div>` in `templates/account.html` (currently only `data-post-id` is set at `templates/account.html:211`). — auto-continue *(resolved 2026-08-31)*
- [x] **`browse.html`'s Search button hand-rolls an inline style instead of using the shared `.stage-actions` class** — `templates/stages/browse.html:48-51` spells out `var(--accent-fg)`/`var(--accent-bg)`/`var(--accent-border)` inline, while every other stage page's primary action button uses the shared class (`configure.html:67`, `connect.html:57/61`, `run.html:41`, `schedule.html:59/63`, `storage.html:103/107`). Fix: replace the inline `style` with `class="stage-actions"` (or equivalent) to match. — auto-continue *(resolved 2026-08-31)*
- [x] **`account.html`'s `.get-more-btn` hardcodes raw colour literals instead of the shared accent tokens** — `templates/account.html:61-65` spells out `#c7d2fe` / `rgba(79,70,229,.65)` / `rgba(49,46,129,.25)`, which exactly duplicate `--accent-fg` / `--accent-border` / `--accent-bg` defined in `static/base.css:15-18`. Fix: reference the CSS variables (or reuse the shared `.stage-actions` class every other stage page's primary button uses). — auto-continue *(resolved 2026-08-31)*
- [x] **`index.html`'s `<title>` reverses the shared page-title order** — `templates/index.html:6` renders `Social Media Archiver — Dashboard`, while every other page (`account.html:6`, `stages/browse.html:6`, `configure.html:6`, `connect.html:6`, `run.html:6`, `schedule.html:6`, `storage.html:6`) uses `Page — Social Media Archiver`. Fix: change it to `Dashboard — Social Media Archiver`. — auto-continue *(resolved 2026-08-31)*
- [x] **`account.html`'s `<title>` uses a plain hyphen instead of the em dash every other page uses** — `templates/account.html:6` renders `{{ account }} - Social Media Archiver`, while `index.html` and every `templates/stages/*.html` use `Page — Social Media Archiver` (em dash). Fix: change the separator in `account.html`'s `<title>` to `—` for consistency. — auto-continue *(resolved 2026-08-31)*
- [x] **`account.html` is the only page missing the shared tab nav** — every other template (`index.html`, all of `templates/stages/*.html`) includes `{% include "_nav.html" %}` for the tab strip added in the staged-tabs refactor (deb7d74); `templates/account.html` still has its own standalone `.header` with a hand-rolled "← Back to Dashboard" link instead. From the account page there's no way to reach Connect/Configure/Storage/Schedule/Run without using the browser back button. Fix: wire `account.html` into `_stage_context`/`_nav.html` like the other stage routes (`web.py:254` `view_account`), matching the pattern used by `stage_browse`/`stage_run`/etc. — auto-continue *(resolved 2026-08-31)*
- [x] **Secondary-link styling disagrees between templates** — decided: neutral is correct. Already delivered by the staged-tab refactor (deb7d74): the indigo "View Posts" CTA no longer exists and `account.html`'s `.back-btn` is the only secondary link, on the neutral/transparent style. Verified 2026-08-23 — no `indigo` classes remain in `templates/`. *(resolved 2026-08-23)*
- [x] **Two "key stats" layouts for the same counts** — decided: borderless row everywhere. `.stat-tile` and its bordered `gap-px` grid are gone; `index.html`, `stages/browse.html` and `account.html` all render one `.stats-bar` row of `.stat-item`s, now defined once in `static/base.css`. `account.html` also drops its duplicated `:root`/body/scrollbar block and links `base.css` instead. All three pages verified 200 with the shared markup. *(resolved 2026-08-23)*
- [x] **Decorative emoji in `templates/index.html` headings** — decided: drop them. Already delivered by the staged-tab refactor; a scan of `templates/**` on 2026-08-23 found no decorative emoji left. The ❤️/🔄/💬 in `account.html`'s `.post-metrics` are kept deliberately — they are the only label those like/retweet/reply counts have, so they carry meaning rather than decoration. *(resolved 2026-08-23)*
- [x] **Dead legacy light-theme CSS block retained in both templates** — `templates/account.html:8-261` and `templates/index.html:8-203` are a full original light-theme stylesheet, entirely superseded by the dark-theme block appended after each (from ~line 263/205 onward). This is the likely source of the stale error colours above; delete the superseded block from both files. — auto-continue *(resolved 2026-08-03)*
- [x] **`loadAccountStats()` is a half-wired stub that duplicates info already shown** — `templates/index.html:515-522` contains the comment `// This would need a new API endpoint, for now just show basic info` and just writes `Platform: ${platform}`, which duplicates the platform badge already rendered at line 503. Either wire it to real stats or remove the stub and its call site. — auto-continue *(resolved 2026-08-02)*
- [x] **Controls-bar margin/radius differ between the two templates** — `templates/account.html:375,377`: `.controls { margin-bottom: 10px; border-radius: 6px; }` vs `templates/index.html:296,300`: `.control-panel { margin-bottom: 12px; border-radius: 7px; }`. Same "action bar" role, unify the values. — auto-continue *(resolved 2026-08-02)*
- [x] **`.header` padding/min-height differ between `account.html` and `index.html` for the same component** — `templates/account.html:307,309`: `min-height: 46px; padding: 8px 12px;` vs `templates/index.html:240-241`: `min-height: 48px; padding: 8px 14px;`. Same page-header bar, unify the values. — auto-continue *(resolved 2026-08-02)*
- [x] **`templates/index.html` error status uses stale light-theme colours against a dark UI** — `startArchiving()`'s catch block (`templates/index.html:551-552`) hardcodes `status.style.background = '#f8d7da'; status.style.color = '#721c24';`, the old light-theme error palette. The rest of the app has moved to a dark theme with `.error` styled via dark tokens (`templates/account.html:446-450`: `rgba(127,29,29,.25)` / `#fca5a5`). `index.html` has no dark-theme error/status class at all, so this fallback path renders a light pink/maroon box against the dark UI. Replace with the same dark-theme error styling `account.html` uses. — auto-continue *(resolved 2026-08-01)*

- [x] **Facebook browser tiers were brittle** — removed authenticated and public
  DOM scraping entirely. Facebook now uses only the official Graph API and
  requires `FB_GRAPH_TOKEN`; unavailable credentials skip the provider without
  blocking other targets. This removes unstable selectors, saved session
  cookies, and misleading best-effort behavior. *(resolved 2026-07-24)*
- [x] **Two runtime generations** — removed the legacy `src/` implementation,
  JSON configuration, channel list, and `archiver.py`; the dashboard now starts
  `core.orchestrator` through `main.py`. *(resolved 2026-07-24)*
- [x] **Storage was filesystem-only** — added a storage factory and optional
  S3-compatible mirror while preserving the local dashboard cache. *(resolved
  2026-07-24)*
- [x] **Dashboard did not browse orchestrator output** — normalized output
  adapter added. *(resolved 2026-07-07)*
- [x] **Twitter path lived outside the connector architecture** — added the
  Nitter RSS connector with instance fallback. *(resolved 2026-07-12)*
- [x] **Reddit fallback was silent** — fallback warnings now explain reduced
  metadata and credential setup. *(resolved 2026-06-12)*
- [x] **Monolith lacked provider isolation** — split source logic into
  `connectors/` and generic plumbing into `core/`. *(resolved 2026-06-06)*
